# 待办事项

这个清单基于当前项目的实际状态，优先覆盖“先跑稳本地 demo，再逐步接入真实模型和推流能力”的路线。

## P0 - 核心链路

- 将当前合成兜底 TTS 替换为可插拔的真实 `CosyVoice` 适配器。
- 增加模型路径发现和配置层，方便本地加载真实模型文件。
- 将当前占位式预览渲染拆分成真正的视频合成阶段。
- 接入 `MuseTalk` 作为主口型驱动后端。
- 保留 `Wav2Lip` 作为兜底后端，便于兼容和故障恢复。
- 把任务生命周期拆得更细，至少区分 TTS、口型合成和视频输出阶段。

## P1 - 使用体验

- 将任务历史持久化到磁盘，避免服务重启后丢失记录。
- 在 Web 控制台里补充更明确的错误展示和重试入口。
- 增加模型和后端切换能力，便于测试不同方案。
- 增加常用商品话术或带货脚本预设。
- 优化预览区布局，让它更适合长时间演示和反复调试。

## P2 - 推流与导出

- 增加 `FFmpeg` 导出预设，支持录制和归档。
- 增加适配 `OBS` 的输出选项，支持虚拟摄像头或 RTMP 工作流。
- 将本地预览和正式导出拆开，分别调优。
- 补充低延迟参数，用于优化首帧启动和连续播放体验。

## P3 - 可观测性

- 记录各阶段耗时，包括 TTS、口型合成、编码和总耗时。
- 在接入 CUDA 模型后，记录 GPU 和显存占用。
- 增加一个简洁的健康检查和指标接口。
- 增加结构化日志，方便排查任务提交、取消和后端选择过程。

## P4 - 文档

- 补充 Windows GPU 环境的安装说明。
- 补充本地模型下载和放置方式。
- 补充 `models/` 和 `assets/` 的目录约定。
- 增加一份快速上手说明，最好配合截图或预览示例。