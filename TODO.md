# 待办事项

这个清单基于当前项目的实际状态，优先覆盖“先跑稳本地 demo，再逐步接入真实模型和推流能力”的路线。

## P0 - 核心链路

### 已完成

- [x] 增加模型路径发现与配置层，统一管理 `assets/`、`models/` 和输出目录。
- [x] 新增 `CosyVoice` 真实推理桥接脚本，支持官方仓库和 `pretrained_models` 权重目录。
- [x] 将 TTS 适配层拆成可插拔架构，优先 `CosyVoice`，再回退到系统语音和合成兜底。
- [x] 将口型视频适配层拆成可插拔架构，优先 `MuseTalk`，再回退到 `Wav2Lip` 和本地 demo 预览。
- [x] 将任务生命周期拆成 `TTS / 口型合成 / 最终编码` 三个阶段。
- [x] 将最终预览输出拆成“视频合成”和“音频编码/封装”两个步骤。

### 进行中

- [ ] 将官方 CosyVoice 仓库和权重在本地环境跑通一轮实际验证。
- [ ] 接入真实 `MuseTalk` 推理脚本与权重文件。
- [ ] 接入真实 `Wav2Lip` 推理脚本与权重文件。
- [ ] 完善模型命令参数约定，减少不同开源项目之间的适配成本。
- [ ] 将本地 demo fallback 收敛成仅开发模式可用的兜底路径。

## P1 - 使用体验

- [ ] 将任务历史持久化到磁盘，避免服务重启后丢失记录。
- [ ] 在 Web 控制台里补充更明确的错误展示和重试入口。
- [ ] 增加模型和后端切换能力，便于测试不同方案。
- [ ] 增加常用商品话术或带货脚本预设。
- [ ] 优化预览区布局，让它更适合长时间演示和反复调试。

## P2 - 推流与导出

- [ ] 增加 `FFmpeg` 导出预设，支持录制和归档。
- [ ] 增加适配 `OBS` 的输出选项，支持虚拟摄像头或 RTMP 工作流。
- [ ] 将本地预览和正式导出拆开，分别调优。
- [ ] 补充低延迟参数，用于优化首帧启动和连续播放体验。

## P3 - 可观测性

- [ ] 记录各阶段耗时，包括 TTS、口型合成、编码和总耗时。
- [ ] 在接入 CUDA 模型后，记录 GPU 和显存占用。
- [ ] 增加一个简洁的健康检查和指标接口。
- [ ] 增加结构化日志，方便排查任务提交、取消和后端选择过程。

## P4 - 文档

- [ ] 补充 Windows GPU 环境的安装说明。
- [ ] 补充本地模型下载和放置方式。
- [ ] 补充 `models/` 和 `assets/` 的目录约定。
- [ ] 增加一份快速上手说明，最好配合截图或预览示例。

## 补充说明

- [x] CosyVoice 桥接脚本已补上常见官方构造参数回退，降低不同版本仓库的接入门槛。
- [ ] 继续在本地放入官方 CosyVoice 仓库和权重后，跑通一次真实合成并固化成默认验证流程。


## 补充说明

- [x] CosyVoice 官方仓库已克隆到 `models/CosyVoice`。
- [x] CosyVoice-300M-SFT 权重已通过官方 ModelScope 仓库下载到本地。
- [ ] 先补全 CosyVoice 依赖（现在已知缺少 `hyperpyyaml`），然后再做一次真实 CosyVoice 合成验证。


## 最新状态同步（0.0.7）

### P0 已完成

- [x] 官方 `CosyVoice` 仓库已克隆到 `models/CosyVoice`。
- [x] `CosyVoice-300M-SFT` 权重已放置到 `models/CosyVoice/pretrained_models/CosyVoice-300M-SFT`。
- [x] 真实 `CosyVoice` `sft` 推理已在本地跑通，并生成 `outputs/cosyvoice-check.wav`。
- [x] 项目内联调已跑通，`scripts/smoke_demo.py` 已实际命中 `cosyvoice -> demo-preview`。

### P0 下一步

- [ ] 清理当前为本地验证加入的临时兼容层，尽量替换为正式可安装依赖。
- [ ] 为真实 `CosyVoice` 补充固定验证脚本或回归测试入口，避免后续改动把链路带坏。
- [ ] 开始接入真实 `MuseTalk` 推理脚本和权重。
- [ ] 开始接入真实 `Wav2Lip` 推理脚本和权重。


## 最新状态同步（0.0.8）

### P0 已继续收敛

- [x] `pyarrow`、`pyworld` 从仓库中删除，训练数据链路的重依赖不再参与真实 `sft` 启动。
- [x] `modelscope`、`tqdm` 改成可选入口后，已从仓库中删除。
- [x] `CosyVoice` 前端里的 `onnxruntime`、`inflect`、`whisper`、`torchaudio` 相关能力已改成延迟初始化。
- [x] 真实 `CosyVoice` `sft` 推理仍然通过。
- [x] 项目内联调仍然通过。

### P0 仍待完成

- [ ] 继续把剩余的临时兼容层收敛成正式安装依赖，优先处理 `whisper`、`torchaudio`、`onnxruntime`、`inflect`、`hyperpyyaml` 这一组。
- [ ] 如果你能提供可用的网络/镜像/离线 wheel，我再把官方依赖一次性补齐并继续删替身。
- [ ] 接着把真实 `MuseTalk` 推理链路接入。
- [ ] 接着把真实 `Wav2Lip` 推理链路接入。


## 最新状态同步（0.0.9）

### P0 已继续收敛

- [x] 通过 Clash + 清华镜像，`pip` 已恢复正常可用。
- [x] 官方 `CosyVoice` 依赖已安装进虚拟环境。
- [x] 仓库根目录里大部分临时兼容层已删除，导入行为回到官方包。
- [x] 真实 `CosyVoice` `sft` 推理仍然通过。
- [x] 项目内联调仍然通过。

### P0 仍待完成

- [ ] 再检查一次 `scripts/cosyvoice_bridge.py` 和 `models/CosyVoice` 里剩下的少量 vendor 级补丁，看看是否还能继续收敛。
- [ ] 视需要补一个更稳定的本地回归测试，专门覆盖真实 `CosyVoice` 启动与产音。
- [ ] 接着把真实 `MuseTalk` 推理链路接入。
- [ ] 接着把真实 `Wav2Lip` 推理链路接入。


## 最新状态同步（0.1.0）

### P0 已完成

- [x] 通过 Clash + 清华镜像，`pip` 已恢复正常可用。
- [x] 官方 `CosyVoice` 依赖已安装进虚拟环境。
- [x] 仓库根目录里大部分临时兼容层已删除，导入行为回到官方包。
- [x] 真实 `CosyVoice` `sft` 推理仍然通过。
- [x] 项目内联调仍然通过。

### P0 下一步

- [ ] 再补一个更稳定的本地回归测试，专门覆盖真实 `CosyVoice` 启动与产音。
- [ ] 继续把 `models/CosyVoice` 里剩下的少量 vendor 级补丁再收敛一轮。
- [ ] 接着把真实 `MuseTalk` 推理链路接入。
- [ ] 接着把真实 `Wav2Lip` 推理链路接入。


## 最新状态同步（0.1.1）

### P0 已完成

- [x] 真实 `CosyVoice` 合成链路再次复测通过。
- [x] 项目内联调链路再次复测通过。
- [x] 当前文档内容已同步更新到最新验证结果。

### P0 下一步

- [ ] 给真实 `CosyVoice` 再补一条更稳定的回归测试入口，后续改动可以更快发现回退。
- [ ] 继续把 `models/CosyVoice` 里剩下的少量 vendor 级补丁再收敛一轮。
- [ ] 接着把真实 `MuseTalk` 推理链路接入。
- [ ] 接着把真实 `Wav2Lip` 推理链路接入。
